version: "3.9"

services:
  fraud-platform:
    image: python:3.11
    working_dir: /app
    volumes:
      - .:/app
    command: bash -c "
      pip install -r requirements.txt &&
      streamlit run dashboard/streamlit_app.py --server.address 0.0.0.0
      "
    ports:
      - "8501:8501"
